interface:
  display_name: "Skill Dev"
  short_description: "Author portable repo-local shared skills and adapter metadata in tusk"
  default_prompt: "Use $skill-dev to author portable `.agents/skills/*` bundles, keep runtime overlays such as `agents/openai.yaml` adapter-specific, fold useful upstream creator patterns into the repo-owned loop, and treat staging or publish helpers as runtime adapters."

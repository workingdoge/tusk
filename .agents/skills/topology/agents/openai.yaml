interface:
  display_name: "Topology"
  short_description: "Place work and shape one clean lane wire"
  default_prompt: "Use $topology to decide where consequential work or tracker/runtime drift belongs, shape one bounded wire, and only then hand execution to $tusk."
